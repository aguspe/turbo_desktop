# Node
node_modules/

# Rust / Tauri build artifacts
src-tauri/target/

# Rails logs / tmp
*.log
turbo_desktop-rails/log/*
!turbo_desktop-rails/log/.keep
turbo_desktop-rails/tmp/

# Editor / LSP caches
.ruby-lsp/
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Local scratch / test scaffolds
debug_project/
test_raider_project/

# Local clone of the separate example-app repo (github.com/aguspe/turbo_desktop_example_app)
turbo_desktop_example_app/

# Built gem artifacts — ship via RubyGems / GitHub Releases, not git
turbo_desktop-rails/*.gem

# Gem test scratch output
turbo_desktop-rails/test/tmp/

# Local desktop run config (per-app / per-user; not part of the shell source)
/turbo-desktop.config.json

# Browser session scratch from local gem test runs
turbo_desktop-rails/.browser-sessions/

# Generated code graph (rebuild with `graphify update .`)
graphify-out/

# Tauri ACL schemas — regenerated by tauri-build on every cargo run
src-tauri/gen/schemas/

# Command permissions generated from APP_COMMANDS in build.rs
src-tauri/permissions/autogenerated/
